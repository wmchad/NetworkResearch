## Plots the volume of many towers over time
## against the distance from a given location
## (heatmap type)
PlotVolumeVsDistVsTime <- function(towers, lat, long,
                                   startTime, endTime,
                                   eventTime=NULL,
                                   plotTitle=NULL) {
}

## Plots volume between a given tower and all other towers
## Heatmap with time on one axis, distance on the other
PlotSingleTowerVolumeVsDistVsTime <- function(towerId, lat, long,
                                              startTime, endTime,
                                              eventTime=NULL,
                                              plotTitle=NULL) {
}
                                               
